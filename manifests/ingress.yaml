apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myapp-web
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: myapp-web
            port:
              number: 80
